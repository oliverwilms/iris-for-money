version: '3.8'
services:
  iris:
    build: 
      context: .
      dockerfile: Dockerfile
    #command: "-b /opt/irisapp/vcopy.sh"
    command: --check-caps false -b /opt/irisapp/vcopy.sh
    init: true
    restart: always
    ports:
      - 19800:1972
      - 57700:52773
      - 53773
    volumes:
# no license required for community image    
#      - ~/iris.key:/usr/irissys/mgr/iris.key
      - riches:/voldata

volumes:
  riches:
